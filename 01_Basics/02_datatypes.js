"use strict" // treats all code as newer version of JavaScript

// alert(3+3) // we are using it on nodejs, so alert won't work, but it works in browsers

console.log(3+3)

console.log("Kanishk")

let name = "Kanishk" 
let age = 18
let IsUserLoggedIn = true

console.log(typeof null)